# 🧹 SwiftLint Setup

This project uses **[SwiftLint](https://github.com/realm/SwiftLint)** to enforce consistent Swift style and conventions.

---

## 📦 Installation (Required)

To ensure clean builds and consistent code style, please install **SwiftLint** via [Homebrew](https://brew.sh):

```bash
brew install swiftlint
